Building a slot machine 

Create a new folder 
Create a new file inside that folder named "project.js" 

Open a terminal in side the folder and run the folowing comands:
    npm init
    npm i prompt-sync

